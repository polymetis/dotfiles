# Global gitignore (core.excludesfile). Keep this conservative — it applies to
# every repo, so only list things that should NEVER be committed anywhere.

# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Editor / tooling cruft
*~
*.swp
*.swo
tags

# Claude Code local settings (machine-specific)
**/.claude/settings.local.json
